package com.mediamate.user.role.tenant;

import com.mediamate.user.role.UserRole;

public class Tenant extends UserRole {
    public Tenant(String firstName, String lastName) {
        super(firstName, lastName);
    }
}
